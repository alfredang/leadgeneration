// Local config — copy this file to `config.js` and paste your Apify API token below.
// Get it from: https://console.apify.com/account/integrations
// `config.js` is gitignored so your token stays out of source control.
window.APIFY_TOKEN = "";
